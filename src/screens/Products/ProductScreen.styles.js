import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    flex: 1,
    resizeMode: "contain",
    height: 400,
    width: "100%",
    marginTop: 20,
  },
  content: {
    marginHorizontal: 40,
  },
});
