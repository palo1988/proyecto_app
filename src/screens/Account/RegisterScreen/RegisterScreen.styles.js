import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    resizeMode: "cover",
    width: "100%",
    height: 300,
    marginTop: 0,
  },
  content: {
    marginHorizontal: 20,
  },
});
